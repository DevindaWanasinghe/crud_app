import React from 'react';
import { View, Text } from 'react-native';

const ProfileScreen = () => {
  return (
    <View className="items-center justify-center flex-1 bg-gray-200">
      <Text className="text-3xl font-bold">Profile Screen</Text>
    </View>
  );
};

export default ProfileScreen;
